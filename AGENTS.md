# epmerchant 商家端入口规则

每次任务必须先读：

- `../ep-shared/PROJECT_RULES.md`
- `.cursorrules`

本仓库职责：ExpatTH 商家端 Next.js。商家端长期对象是 `MerchantCapability`、`MerchantCandidate`、`MerchantQuoteConfirmation`。

本文件只做入口。最高准则、文档格式、审计要求、触发阅读方式以 `../ep-shared/PROJECT_RULES.md` 为准。
