# Expath 本地执行规则（前端项目）

## 0. 项目身份（必须牢记）
- 当前项目身份：`expath-merchant` 商家端
- 技术栈：Next.js App Router + TypeScript + next-intl + Tailwind
- 目标：在不破坏现有功能前提下，持续提升可用性、一致性与可维护性。

## 1. 共享单源入口（必须先读）
- 每次任务必读（最小集）：
  - `shared/docs/api-request.md`
  - `shared/docs/api-list.md`
- 关联任务存在时必读：
  - `Linear` 对应任务、状态、最近更新
- 按需读取（条件触发）：
  - 只在用户明确要求时，再读本项目本地 docs 或历史文件
- 本项目设计：`docs/stage2/design.md`

## 2. 日志与文档强制规则
1. 开发前：先看 `shared/docs/api-request.md`、`shared/docs/api-list.md`，再看关联 `Linear` 任务。
2. 新需求或完成更新：必须同步到对应 `Linear` 任务；没有对应任务就新建。
3. 若用户没有明确要求，不主动维护额外 shared 日志文件。
4. 若需要补文字结论，优先回写 `api-request.md` 或 `api-list.md`，不要重新扩张 shared 目录。

## 3. API 协作闭环（前后端）
1. 新需求、差异、待补接口边界，直接写 `shared/docs/api-request.md`。
2. 接口状态、上下线边界，只写 `shared/docs/api-list.md`。
3. 当前 shared 已收口，若接口细节未写明，以后端当前代码和联调结果为准。
4. 新需求必须同步建到 `Linear`；完成更新必须同步回对应 `Linear` 任务。
5. `Linear` 主要承担：新需求、完成更新、进度状态。
6. 商家端页面/API 默认仅使用 `/api/v1/merchant/*`；若缺 merchant 对应接口，先在 `api-request.md` 补需求，禁止直接复用用户端 `/api/v1/auth/*`、`/api/v1/me/*`。
7. 参数命名以后端当前代码和联调结果为唯一事实源：前端禁止自造字段名、禁止猜测别名。
8. 若发现 shared 缺少统一契约说明，先在 `api-request.md` 标注差异，再要求后端明确，不自行猜字段。

## 3.1 工具优先工作流
1. 当前默认采用“本地极简 shared”模式，不再依赖 `Apifox`、`Scalar`、额外共享流程文件。
2. shared 当前只把 `api-request.md`、`api-list.md` 作为主入口。
3. 若用户未明确要求，不要再扩展 shared 文档矩阵。

## 4. 禁止事项
- 禁止未更新日志就宣称任务完成。
- 禁止自行恢复被下线的重型 shared 流程。
- 禁止跨项目改写他人的历史日志条目。
- 禁止仅凭 shared 文档、命名推测或前端需要，自行新增后端接口代理、返回字段、类型契约或联调链路。
- 若联调时发现“文档写了但后端实际没有”或“后端当前返回里没有该字段/接口”，以前端实际请求结果和后端当前代码为准，必须先向用户说明，再决定是回退实现还是在 `api-request.md` 标注差异。
- 未经用户明确确认，不允许为了“先跑通页面”继续添加假设性接口，例如新的 `/api/v1/merchant/*` 子路由、模板规则接口、临时返回字段或本地兼容分支。