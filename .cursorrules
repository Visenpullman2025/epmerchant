# epmerchant 商家端 Cursor 入口规则

每次任务必须先读：`../ep-shared/PROJECT_RULES.md`。

本仓库身份：

- 项目：ExpatTH 商家端。
- 技术栈：Next.js App Router + TypeScript + next-intl + Tailwind。
- 边界：商家端页面、组件、BFF、i18n、样式、商家端数据适配。

仓库特有要求：

- 长期对象是 `MerchantCapability`、`MerchantCandidate`、`MerchantQuoteConfirmation`。
- 不自造商家 API 字段。
- 不强化“服务商品 / 上架商品”旧心智。
- 用户可见文案必须维护 `zh/en/th`。

最高准则、文档格式、触发阅读、越界审计以 `../ep-shared/PROJECT_RULES.md` 为准。
